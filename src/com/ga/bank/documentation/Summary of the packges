
update 1:

/MODEL PACKAGE


/USER PACKAGE

USER.JAVA ABSTRACT CLASS: common user field such as id name passwordhash and role
for now this is it might change later

CUSTOMER.JAVA NORMAL CLASS: this will extend user .represent a bank client holds there failed login count
and lockout timestamp might change later

BANKER.JAVA NORMAL CLASS: this will aslo extends user to represet bank staff whole will have admistrative privaileges like
creating new customers might change later

ROLE.JAVA ENUMS CLASS: this is enum class that will hold the BANKER or CUSTOMER to prevent the dealing with typos and reduce the need to check this input



/ACCOUNT PACKAGE

TRANSACTABLE.JAVA INTERFACE CLASS: this is used to define methods deposite withdraw and transfer that every account class must implement

ACCOUNT.JAVA ABSTRACT CLASS: this will implement TRANSACTABLE to share account status account id balence debtcard status varable that should be known about the account when extend this class

CHECKINGACCOUNT.JAVA NORMAL CLASS: this will exted ACCOUNT to enforce the overdraft logic cap when negative and will deactivate the account when it hits the 2nd withdraw from a negative balence

SAVINGACCOUNT.JAVA NORMAL CLASS : this will extend ACCOUNT enforces the rules of saving account no ovrdraft is allowed

ACCOUNTSTATUS.JAVA ENUM CLASS: this will be used to define the status there are three possible outcomes ACTIVE DEACTIVED LOCKED for the account records
the same reason as with the ROLE CLASS ENUM the same reasoning as will



/CARD PACKAGE

CARDTYPE.JAVA ENUM CLASS: this will define the 3 type of cards we have PLATINUM ,TITANIUM AND STANDARED

DEBITCARD.JAVA NORMAL CLASS: this is the actual card issused to an account holding its unique card number and assigned CARDTYPE



/TRANSACTION PACKAGE


TRANSACTON.JAVA NORMAL CLASS: this is a used to hold events like the id type amount and posttransactionbalence thing of this as the digital recipte of who moved and where did it move

TRANSACTIONTYPE.JAVA ENUM CLASS: this is used to define a fixed DEPOSITE, WITHDRAW ,TRANSFER_IN ,TRANSFER_OUT AND FEE_OVERDRAFT
the same reson we used any enum they all share the same reasoning not dealing with typo and reduce cheking user input code




